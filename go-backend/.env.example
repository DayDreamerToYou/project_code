# 服务器配置
APP_ENV=local
APP_PORT=8080
APP_DEBUG=true

# 数据库配置 (MySQL)
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=fishery
DB_USERNAME=root
DB_PASSWORD=root

# JWT 配置
JWT_SECRET=please-change-this-secret-key-in-production
JWT_EXPIRE_HOURS=24

# CORS 配置
CORS_ALLOW_ORIGINS=*
