# 开发用热重载（air）Dockerfile
FROM golang:1.21-alpine

WORKDIR /app

# 安装 air 热重载工具
RUN go install github.com/air-verse/air@latest

# 复制依赖
COPY go.mod go.sum* ./
RUN go mod download || true

# 复制源代码
COPY . .

EXPOSE 8080

# 启动 air
CMD ["air", "-c", ".air.toml"]
